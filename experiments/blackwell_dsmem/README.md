This directory is kept as a compatibility wrapper for older server-side paths.

The active Blackwell DSMEM / 2SM experiments now live in:

- `experiments/blackwell_dsmem_2sm`

If you run `make` from this directory, the local `Makefile` forwards the target
to `../blackwell_dsmem_2sm`.
